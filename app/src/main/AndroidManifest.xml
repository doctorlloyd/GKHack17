<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.itechhub.datawizards.colabo_ai">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Main"
            android:label="Home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Profile"
            android:label="Profile" />
        <activity
            android:name=".Projects"
            android:label="Home" />
        <activity
            android:name=".ColleaguesMeeting"
            android:label="Home" />
        <activity
            android:name=".CostumeMeetings"
            android:label="Choose Colleague" />
        <activity
            android:name=".Recommended"
            android:label="Recommended Colleagues" />
    </application>

</manifest>